function solution(arr) {
  let newArr = [];
  let count = 0;
  let answer;
  for (let i = 0; i < arr.length; i++) {
    count++;
    newArr.push(arr[i]);
  }
}

let arr = [3, 1, 2, 3, 4];

console.log(solution(arr));
